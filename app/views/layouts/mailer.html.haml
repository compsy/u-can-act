!!!
%html
  %head
    %meta{:content => 'text/html; charset=utf-8', 'http-equiv' => 'Content-Type'}/
    :css
      /* Email styles need to be inline */
      .logo {width: 72px; }

      @font-face {
        font-family: 'Roboto';
        src: url("#{ENV['HOST_URL']}/fonts/Roboto-Bold.eot");
        src: url("#{ENV['HOST_URL']}/fonts/Roboto-Bold.eot?#iefix" %>) format('embedded-opentype'), url("#{ENV['HOST_URL']}/fonts/Roboto-Bold.woff") format('woff'), url("#{ENV['HOST_URL']}/fonts/Roboto-Bold.ttf" %>) format('truetype');
        font-weight: 700;
      }
      @font-face {
        font-family: 'Roboto';
        src: url("#{ENV['HOST_URL']}/fonts/Roboto-Regular.eot");
        src: url("#{ENV['HOST_URL']}/fonts/Roboto-Regular.eot?#iefix" %>) format('embedded-opentype'), url("#{ENV['HOST_URL']}/fonts/Roboto-Regular.woff") format('woff'), url("#{ENV['HOST_URL']}/fonts/Roboto-Regular.ttf" %>) format('truetype');
        font-weight: 400;
      }

      body {
        font-family: 'Roboto', sans-serif;
      }

      h1 {
        font-size: 40px;
        font-weight: 700;
        line-height: 1;
      }

      h2 {
        font-size: 22px;
        font-weight: 700;
      }

      h3 {
        font-size: 18px;
        font-weight: 700;
        color: #243a76;
      }

      h4 {
        font-size: 16px;
        font-weight: 700;
      }

      h5 {
        font-size: 14px;
        font-weight: 700;
      }

      h6 {
        font-size: 14px;
        color: #e6673d;
        font-weight: 700;
      }

      a {
        color: #fff;
        text-decoration: none;
      }

  %body{style: "font-size: 62.5%;font-family: 'Roboto', sans-serif; -webkit-tap-highlight-color: rgba(0, 0, 0, 0);"}
    %div{style: "padding-top: 1px; font-family: 'Roboto', sans-serif; font-size: 16px;line-height: 1.428571429; color: #252828;" }
      .panel{style: "border: 2px solid #ccc; margin: 0px auto; padding: 10px; width: 560px; height: auto; background-color: #ffffff;-webkit-border-radius: 7px;-moz-border-radius: 7px;border-radius: 7px;"}
        %div{style: "padding-top:20px;padding-left:20px;padding-right:20px"}
          - if logo_image.present?
            = image_tag logo_image, class: 'logo', alt: Rails.application.config.settings.application_name
          = yield
    %div{style: "color: #ccc;"} #{"Ref: #{SecureRandom.hex(6)}"}
