= render partial: 'shared/header'
= QuestionnaireGenerator.generate_questionnaire(response_id: @response.id,
  content: @protocol.informed_consent_questionnaire.content,
  title: @protocol.informed_consent_questionnaire.title,
  submit_text: 'Volgende',
  action: '/informed_consent',
  unsubscribe_url: Rails.application.routes.url_helpers.questionnaire_path(uuid: @response.uuid),
  params: { authenticity_token: form_authenticity_token(form_options: { action: '/informed_consent', method: 'post' })})
