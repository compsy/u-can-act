<%= QuestionnaireGenerator.generate_questionnaire(@response,
                                                  form_authenticity_token(form_options: { action: '/',
                                                                                          method: 'post' })) %>
