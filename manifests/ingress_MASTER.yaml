apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: svc-questionnaires
  namespace: sdv-mvp-master
  labels:
    app: svc-questionnaires
spec:
  rules:
  - host: svc-questionnaires.beta.sportdatavalley.nl
    http:
      paths:
      - path: /
        backend:
          serviceName: svc-questionnaires-service
          servicePort: 3000

