describe("MentorPage", function() {
  beforeEach(function() {
    const component = React.createElement(MentorPage, {});
    this.rendered = TestUtils.renderIntoDocument(component)
  });

  describe("defaultProps", function() {
    it("it should ", function() {
      fail('be tested');
    });
  });
});
